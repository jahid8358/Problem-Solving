#include<bits/stdc++.h>
using namespace std;
 
void luffy(){
   int n;
   cin>>n;
   if(n>=1900)
   cout<<"Division 1
";
   else if(n>=1600 && n<=1899)
   cout<<"Division 2
";
   else if(n>=1400 && n<=1599)
   cout<<"Division 3
";
   else
   cout<<"Division 4
";
    return;
}
int main(){
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
 
    int n;
    cin>>n;
    while(n--)
    luffy();
 
    return 0;
}